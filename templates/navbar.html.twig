<nav>
    <div class="container">
        <div class="nav-wrapper">
            <a href="{{ path('home') }}" class="left brand-logo">Share Cheese</a>
            <ul id="nav-mobile" class="right hide-on-med-and-down">
                <li>
                    <a href="{{ path('home') }}">Ads</a>
                </li>
                <li>
                    <a href="{{ path('cheese_index') }}">Cheeses</a>
                </li>
            </ul>
            {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
            <a class="navbar-brand" href="" data-toggle="collapse" data-target="#navbarUserDropdown" aria-controls="navbarUserDropdown" aria-expanded="false" aria-label="Toggle navigation">Hello
                {{ app.user.username }}
                !</a>
            {% endif %}
            {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
            <div class="collapse navbar-collapse" id="navbarUserDropdown">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('user_show', {'id': app.user.id}) }}">My account</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_logout') }}">Logout</a>
                    </li>
                </ul>
            </div>
            {% endif %}
            <form>
                <div class="input-field right">
                    <input id="search" type="search" required="required">
                    <label class="label-icon" for="search">
                        <i class="material-icons">search</i>
                    </label>
                    <i class="material-icons">close</i>
                </div>
            </form>
        </div>
    </div>
</nav>
